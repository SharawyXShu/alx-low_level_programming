#include "main.h"
#include <stdio.h>

/**
 * main - check the code for
 *
 * Return: Always 0.
 */
int main(void)
{
	char s[1] = "Expect the best. Prepare for the worst";
	char s[2] = "Capitalize on what comes.\n";
	char *p;

	p = leet(s);
	printf("%s", p);
	printf("%s", s1);
	printf("%s , s2);
	return (0);
}
