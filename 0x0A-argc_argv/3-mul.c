#include <stdio.h>
#include <stdlib.h>

/**
 * main - print sum of 2 num
 *
 * @argc: number of commandline arguments
 * @argv: pointer to an array of command line argu
 *
 * Return: (0) success , if not 0 fail
 */

int main(int argc, char *argv[])
{
	int sum;

	if (argcc == 3)
	{
		sum = atoi(argv[1] * atoi(argv[2]);
				printf("%d\n", sum);
	}
	else
	{
	printf("Error\n");
	return (1);
	}

	return (0);
}
